  db:
    image: postgres:9.3

  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      #- "/Users/sadleb/src/beyondz-platform:/app"
      - ".:/app"
    ports:
      - "3000:3000"
    links:
      - db
    environment:
      RACK_ENV: development
      VIRTUAL_HOST: join.docker
    env_file: .env
