      <div class="plank tertiary-md light">
        <div class="section-container">
      <section>
        <div class="col-sm-12">
          <h1>
            Choose Your University
          </h1>
          </div>
        </section>
      </div>
      </div>


  <div class="plank plain-lt">
    <div class="section-container">
      <section>
      <div class="col-sm-12">

        <div style="min-height: 600px;">
        <%= form_tag :action => 'university_update' do |f| %>
          <% List.find_by_friendly_name('universities').items.each do |school| %>
            <div>
              <%= radio_button_tag :university_name, school, false, :required => 'required' %>
              <%= label_tag "university_name_#{school}".to_sym, school %>
            </div>
          <% end %>
          <button class="button-primary" type="submit" name="user_submit">Next Step</button>
        <% end %>
        </div>

      </div>
      </section>
    </div>
  </div>
