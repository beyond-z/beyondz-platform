<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Braven" %></title>
    <meta name="description" content="<%= yield(:description) || "Braven" %>">
    
    <!-- Until we add a landing page and login, keep our site out of search engines -->
    <meta name="robots" content="noindex">
    <%= render 'shared/asset_stylesheet_includes' %>
    <%= csrf_meta_tags %>
    <script>
        // set this as soon as possible to avoid flash of content in an iframe
        if(window != window.top)
          document.documentElement.className += ' in-iframe';
    </script>
  </head>
  <body>

    <%= render 'shared/standard_modals' %>
    <%= yield :modals %>

    <div id="content">
      <header class="hide-on-iframe">
        <%= render 'shared/header' %>
        <%= render 'shared/navigation' %>
      </header>
      <main role="main">
        <%= render 'shared/messages' %>
        <%= yield %>
      </main>

      <%= render 'shared/footer' %>
      <%= render 'shared/feedback_button' %>
    </div>
    
    <%= render 'shared/asset_javascript_includes' %>
    <%= render 'shared/google_analytics' %>
  </body>
</html>
