<div id="new-referral-page">
  <div class="plank plain-lt">
    <div class="section-container">
      <section>
        <div class="col-sm-6">

<h1>Refer a <%= @referral_type_text %> to Braven</h1>

<p class="lead"></p>
<div class="panel-body">
  <%= form_for @referral, role: 'form' do |f| %>
    <%= hidden_field_tag 'referral[referring_type]', @referral_type %>
    <%= render :partial => 'shared/form_errors', locals: { object: @referral } %>
    <fieldset>
      <legend>Location</legend>
      <p>First, we need to know which Braven location you are referring to.</p>

      <label class="required">Referral Location:</label>
      <div class="form-group">
        <% areas = List.find_by_friendly_name((defined? list) ? list : 'universities')
           unless areas.nil?
           areas.items.each_with_index do |region, idx| %>
          <div>
            <label>
              <%= f.radio_button :referral_location, region, "required" => "required" %>
              <span><%= region %></span>
            </label>
          </div>
        <% end %>
        <% end %>
      </div>

    </fieldset>

    <fieldset>
      <legend>About You</legend>
      <p>Now, we'd like to know a little about you.</p>
      <div class="form-group">
        <section>
          <div class="col-sm-6">
            <%= f.label :referred_by_first_name, 'Your Name', :class => 'required' %>
            <%= f.text_field :referred_by_first_name, :class => 'form-control', required: 'required', placeholder: 'First Name', style: 'float: left;' %>
          </div>
          <div class="col-sm-6">
            <label>&nbsp;</label>
            <%= f.text_field :referred_by_last_name, :class => 'form-control', required: 'required', placeholder: 'Last Name', style: 'float: left;'  %>
          <div>
        </section>
      </div>
      <div class="form-group">
        <section>
          <div class="col-sm-8">
            <%= f.label :referred_by_email, "Your Email Address", :class => 'required' %>
            <%= f.text_field :referred_by_email, :type => 'email', :required => 'required', :class => 'form-control', placeholder: 'Email'%>
          </div>
        </section>
      </div>
      <div class="form-group">
        <section>
          <div class="col-sm-8">
            <%= f.label :referred_by_phone, "Your Phone Number (optional)" %>
            <%= f.text_field :referred_by_phone, :type => 'phone', :class => 'form-control', placeholder: 'Phone number'%>
          </div>
        </section>
      </div>
      <div class="form-group">
        <section>
          <div class="col-sm-8">
            <%= f.label :referred_by_employer, "Your Employer (optional)" %>
            <%= f.text_field :referred_by_employer, :type => 'text', :class => 'form-control', placeholder: 'Employer'%>
          </div>
        </section>
      </div>

      <div class="form-group">
        <section>
          <div class="col-sm-10">
            <%= f.label :referred_by_affiliation, "What's your affiliation to Braven?", :class => 'required' %>
            <div class="clickable-option with-text-field">
              <label><%= f.radio_button :referred_by_affiliation, 'Previous Braven Fellow', required: 'required' %> Previous Braven Fellow</label><br />
              <label><%= f.radio_button :referred_by_affiliation, 'Previous Braven Leadership Coach or Volunteer', required: 'required' %> Previous Braven Leadership Coach or Volunteer</label><br />
              <label><%= f.radio_button :referred_by_affiliation, 'University faculty/staff', required: 'required' %> University faculty/staff</label><br />
              <label><%= f.radio_button :referred_by_affiliation, 'Student organization leader', required: 'required' %> Student organization leader</label><br />
              <label><%= f.radio_button :referred_by_affiliation, 'K-12 champion (e.g. KIPP, College Track, etc.)', required: 'required' %> K-12 champion (e.g. KIPP, College Track, etc.)</label><br />
              <label><%= f.radio_button :referred_by_affiliation, 'other', required: 'required' %> Other</label><br />
            </div>
          </div>
        </section>
      </div>

    </fieldset>

    <fieldset>
      <legend>About the person you're referring</legend>

      <p>Finally, we need to know how to contact the person you think would make a good Braven <%= @referral_type_text %>.</p>

      <div class="form-group">
        <section>
          <div class="col-sm-6">
            <%= f.label :referred_first_name, 'Their Name', :class => 'required' %>
            <%= f.text_field :referred_first_name, :class => 'form-control', required: 'required', placeholder: 'First Name', style: 'float: left;' %>
          </div>
          <div class="col-sm-6">
            <label>&nbsp;</label>
            <%= f.text_field :referred_last_name, :class => 'form-control', required: 'required', placeholder: 'Last Name', style: 'float: left;'  %>
          <div>
        </section>
      </div>
      <div class="form-group">
        <section>
          <div class="col-sm-8">
            <%= f.label :referred_email, "Their Email Address", :class => 'required' %>
            <%= f.text_field :referred_email, :type => 'email', :required => 'required', :class => 'form-control', placeholder: 'Email'%>
          </div>
        </section>
      </div>
      <div class="form-group">
        <section>
          <div class="col-sm-8">
            <%= f.label :referred_phone, "Their Phone Number (optional)" %>
            <%= f.text_field :referred_phone, :type => 'phone', :class => 'form-control', placeholder: 'Phone number'%>
          </div>
        </section>
      </div>


    </fieldset>


    <div class="form-group">
      <button type="submit" class="button-primary">Submit</button>
    </div>

  <% end %>
</div>


        </div>
      </section>
    </div>
  </div>
</div>
